{
  "name": "olympus-subgraph",
  "version": "0.1.0",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ tamdao",
    "create-local": "graph create --node http://localhost:8020/ tamdao",
    "remove-local": "graph remove --node http://localhost:8020/ tamdao",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 tamdao"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.21.0",
    "@graphprotocol/graph-ts": "^0.20.0"
  }
}
